import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vinicius.marketlist',
  appName: 'Market_List_App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
